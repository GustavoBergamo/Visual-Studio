using System;
using System.Data.SqlClient;

public bool Cadastrar (int id, string nome, long cpf, string email, string senha){
    string query = "INSERT into Cliente (id, nome, cpf, email, senha) VALUES ('Default, '" + nome + "', '" + cpf + "', '" + email + "', '" + senha + "')";
    
}